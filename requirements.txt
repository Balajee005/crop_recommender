# Core ML
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
imbalanced-learn>=0.11.0  # optional SMOTE support

# Boosting (choose one)
xgboost>=2.0.0            # install if available; replace with GradientBoostingClassifier if not

# Backend
flask>=3.0.0

# Optional: for notebook-based EDA
matplotlib>=3.7.0
seaborn>=0.12.0
